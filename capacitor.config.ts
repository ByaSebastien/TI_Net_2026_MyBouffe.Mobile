import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'TI_Net_2026_MyBouffe.Mobile',
  webDir: 'www'
};

export default config;
